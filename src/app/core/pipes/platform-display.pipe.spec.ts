import { PlatformDisplayPipe } from './platform-display.pipe';

describe('PlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
